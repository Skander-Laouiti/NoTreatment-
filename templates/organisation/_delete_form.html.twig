<form method="post" action="{{ path('app_organisation_delete', {'id': organisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ organisation.id) }}">
    <button class="btn btn-danger" type="submit">DELETE</button>
</form>

